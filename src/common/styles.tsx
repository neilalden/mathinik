import { StyleSheet } from "react-native"

export const Style = StyleSheet.create({
    MegaTitle: {
        color: '#000',
        fontSize: 40,
        textAlign: 'center',
        fontWeight: '900',
        textTransform: 'uppercase',
    }
})